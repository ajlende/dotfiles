" Only actually useful if you load with the -u flag
set nocompatible 

set number
set tabstop=2
set showcmd

set hlsearch
set ignorecase
set smartcase
set incsearch

syntax enable
set background=dark
colorscheme base16-ngeva

